import type { NextConfig } from "next";

const nextConfig: NextConfig = {
   images: {
    domains: ['images.unsplash.com',"undeleterious-alecia-checked.ngrok-free.dev","varivo.duckdns.org"], 
  },
};

export default nextConfig;
